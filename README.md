# Uso básico de Formularios en NextJS

Ejemplo de uso de *acciones de servidor* desde un formulario.


Hacemos uso de las siguientes propiedades:

- **`action`** ( en `form` )
- **`formAction`** ( en `button` )